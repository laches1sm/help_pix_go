package main

func main() {
	// TODO: Move this stuff into it's own folder
	// Add webserver setup stuff here
	// Set up firebase stuff
	// 	// set up env var for API key, idk what is is in go?
	//     api_key := os.Getenv("RIOT_API_KEY")
	// 	// change up region, might want to also make this an env var?
	// 	client := golio.NewClient(api_key,
	//                 golio.WithRegion(api.RegionNorthAmerica),
	//                 golio.WithLogger(logrus.New().WithField("test",  "bar")))
	// 	// get the summoner we're trying to look up info for. Good idea for a cli tool to take in both summoner name and regions?
	// 	summoner, err := client.Riot.Summoner.GetByName("")
	// 	if err != nil{
	// 		fmt.Printf("Summoner not found.")
	// 	}
	// 	// Now for the fun stuff! Time to filter by champ
}
